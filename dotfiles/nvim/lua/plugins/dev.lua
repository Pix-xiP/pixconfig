-- dev.lua - all the configuration for plugins that are either being trialed, or need hotfixes temporarily.

-- It should be reviewed often to check if we can fix, update, or remove stuff that wasn't useful.
return {
	-- quick fixes for mason 2.0 breaking changes in lazy
	-- { "mason-org/mason.nvim", version = "1.11.0" },
	-- { "mason-org/mason-lspconfig.nvim", version = "1.32.0" },
}
