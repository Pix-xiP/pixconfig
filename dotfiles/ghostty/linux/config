# vim: set filetype=ghostty :
#
# Ghostty config :D
#
# Pix's Config
## Hotreload with CMD SHIFT ,

# Theme:
#theme = pink-n-purple-pix
theme = Umbral

config-file = keybinds
 
# background-opacity = 0.85
background-opacity = 1
background-blur-radius = 30
unfocused-split-opacity = 1

## FONT:
font-family = "Maple Mono"
font-size = 14
font-feature = cv01
#font-feature = cv02
font-feature = ss01
font-feature = ss02
font-feature = ss03
font-feature = ss04
font-feature = ss05

# Maple Mono v7 when it drops.
font-feature = zero #: dot style 0
font-feature = cv01 #: Classic @ / $ / & / Q / => / ->
font-feature = cv02 #: Alternative a (with top alarm)
font-feature = cv03 #: Alternative i (without bottom left bar)
font-feature = cv04 #: Alternative l (with bottom left bar) and 1 (without bottom bar)
font-feature = cv98 #: Full width …(ellipsis) and —(emdash) support for Maple Mono NF CN
font-feature = cv99 #: Traditional punctuations support for Maple Mono NF CN
font-feature = ss01 #: Non-cursive italic style l i j k x
font-feature = ss02 #: Disable ligautures on equals like == / != / <=
font-feature = ss03 #: Ignore cases on all tags
font-feature = ss04 #: Disable ligatures on __, #__, ***, \n

# minimum-contrast = 1.1

# Cursor 
cursor-style = block
cursor-style-blink = false

# Window 
window-padding-x = 5
window-padding-y = 2
window-theme = ghostty

window-decoration = false
gtk-titlebar = false

# QoL || Misc
copy-on-select = clipboard
mouse-hide-while-typing = true
quit-after-last-window-closed = true

window-inherit-working-directory = true
window-inherit-font-size = true
working-directory = home 

# Shell specifics
shell-integration = fish
shell-integration-features = cursor,sudo,no-title

# Scrollback limit default is 1Mb. Make it 100Mb.
scrollback-limit = 1000000000
image-storage-limit = 1280000000
